{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A simple URL shortener",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/forevertz/url-shortener.git"
  },
  "keywords": [
    "url",
    "shortener",
    "url-shortener",
    "short-url"
  ],
  "author": "Improvertz",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production node .",
    "dev": "docker-compose -f docker-compose.dev.yml up -d && REDIS=//localhost:6379 node .",
    "stop": "docker-compose -f docker-compose.dev.yml -f docker-compose.yml down"
  },
  "devDependencies": {
    "prettier": "^1.14.2",
    "standard": "^12.0.1"
  }
}
